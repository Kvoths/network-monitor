import { CommandStringPipe } from './command-string.pipe';

describe('CommandStringPipe', () => {
  it('create an instance', () => {
    const pipe = new CommandStringPipe();
    expect(pipe).toBeTruthy();
  });
});
